git config --global user.name "Linxq"
git config --global user.email "563122298.com"
git status //查看
git add *.* //提交到暂存区
git commit *.*//提交仓库区
git log //查看提交记录
git log --pretty=oneline //查看提交记录,忽略具体信息
git reset --hard HEAD^ //回退到上个版本,^^回退到上个版本
git reflog //查看版本号
git reset -- hard *(版本号) //查询对应版本后回退到对应版本
git checkout --*.* //撤销修改